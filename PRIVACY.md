# 개인정보처리방침

CCFOLIA 테마 커스터마이저는 다음을 지킵니다.

- **수집하는 개인정보 없음.** 이름, 이메일, 계정 정보, 사용 통계 등 어떤 것도 수집하지 않습니다.
- **네트워크 요청 없음.** 이 확장 프로그램은 외부 서버로 어떤 데이터도 전송하지 않습니다. 분석(analytics), 광고, 원격 코드 로딩이 전혀 없습니다.
- **로컬 저장만 사용.** 사용자가 고른 색상 값은 `chrome.storage.local`을 통해 브라우저 안에만 저장됩니다. 이 데이터는 기기 밖으로 나가지 않으며, 확장 프로그램을 제거하면 함께 삭제됩니다.
- **권한 범위.** `storage` 권한(로컬 저장용)과 `https://ccfolia.com/*` 호스트 권한(테마 CSS를 주입하기 위함)만 사용합니다. 다른 사이트의 데이터에는 접근하지 않습니다.
- **제3자 라이브러리 없음.** 순수 HTML/CSS/JavaScript로만 작성되어 있으며, 외부 패키지나 CDN 스크립트를 전혀 불러오지 않습니다.
